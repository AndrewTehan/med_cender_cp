<h1>Doctors</h1>

<ul>
  <% @doctors.each do |doctor| %>
    <li>
      <%= link_to doctor.full_name, medical_center_department_doctor_path(@medical_center, @department, doctor) %>
    </li>
  <% end %>
</ul>

<hr>

<%= link_to "Departament #{@department.name}", medical_center_department_path(@medical_center, @department) %>
